import React from 'react';
import { Filters } from '../../../features';

// Простий прокс-компонент для Filters
const FiltersPanel = (props: any) => <Filters {...props} />;

export default FiltersPanel;
